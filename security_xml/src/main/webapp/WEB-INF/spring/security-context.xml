<?xml version="1.0" encoding="UTF-8"?>
<beans:beans
	xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
                                 http://www.springframework.org/schema/beans/spring-beans.xsd
                                 http://www.springframework.org/schema/security
                                 http://www.springframework.org/schema/security/spring-security.xsd">

	<!-- security 적용하지 않는 패턴설정 -->
	<http pattern="/**/*.js" security="none" />
	<http pattern="/**/*.css" security="none" />
	<http pattern="/images/**" security="none" />
	
	<!-- 
	auto-config="true" // 자동 설정 사용 여부
	
	<form-login/>
	<http-basic/>
	<logout/>
	 
	-->	
	<http auto-config="true">
		<!-- 
		*설정시 유의사항
		구체적인 intercept-url을 먼저 적어주고 비교적 덜 구체적인 url을 나중에 설정한다. 
		-->
		<!-- securitu가 감시할 URL과 접근가능한 권한을 설정 -->
		<intercept-url pattern="/admin/**" access="ROLE_ADMIN" />
		<intercept-url pattern="/user/**" access="ROLE_USER" />
	</http>
	
	<!-- manager는 인증에 관정 정보를 설정 -->
	<authentication-manager>
		<!-- provider는 실제 인증을 주관 -->
		<authentication-provider>
			<user-service>
				<user name="testA" password="aaa" authorities="ROLE_ADMIN" />
				<user name="testB" password="bbb" authorities="ROLE_USER" />
			</user-service>
		</authentication-provider>
	</authentication-manager>

</beans:beans>
